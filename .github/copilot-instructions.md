# Copilot Instructions

Read `CONTRIBUTING.md` first. It is the canonical workflow contract for this
repository.

Use `AGENTS.md` for the repo map, keep GitHub as the live source of truth, and
follow the canonical branch format:

```text
<actor>/<type>/<scope>/<task>-<id>
```
